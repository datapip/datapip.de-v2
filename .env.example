# SMTP credentials for the contact form (src/lib/contact.ts).
# Without these the form renders and validates, but reports a
# configuration error instead of sending.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=
TO_EMAIL=
